# 混元AI播客生成系统 - 使用指南

## 系统启动成功！

看到以下信息说明系统已成功启动：

```
播客生成器初始化完成！
🚀 混元AI播客生成系统已启动！
📡 访问地址: http://0.0.0.0:7861
```

## 访问WebUI

打开浏览器，访问：**http://localhost:7861**

如果是在远程服务器上运行，访问：**http://服务器IP:7861**

## 使用步骤

### 📝 多角色互动播客（子题目1）

**功能**：将文本素材转化为多角色自然互动的播客音频

**步骤：**

1. **输入文本**
   - 在"播客文本"文本框中输入包含角色标记的文本
   - 格式示例：
     ```
     [角色A]大家好，欢迎收听今天的播客。
     [角色B]是的，今天我们要聊一个很有趣的话题。
     [角色A]没错，让我们开始吧！
     ```
   - **重要**：必须使用 `[角色名]` 格式标记每个角色

2. **上传音色文件**
   - 为每个角色上传对应的音色参考音频（WAV格式）
   - 第一个角色 → "角色A音色"
   - 第二个角色 → "角色B音色"
   - 第三个角色（可选）→ "角色C音色"
   - **提示**：可以使用 `index-tts/examples/` 目录下的示例音频文件

3. **设置参数（可选）**
   - 角色切换静音间隔：默认300毫秒，可根据需要调整

4. **生成播客**
   - 点击"生成播客"按钮
   - 等待生成完成（可能需要几分钟）
   - 在"生成的播客音频"区域播放或下载音频

### 👥 自定义角色播客（子题目2）

**功能**：根据用户自定义的角色人设和音色生成契合风格的播客音频

**步骤：**

1. **设置角色信息**
   - 为每个角色设置：
     - 角色名称（如：主持人、专家、嘉宾）
     - 角色人设描述（如："一个幽默风趣的主持人，喜欢开玩笑"）
     - 音色参考音频

2. **输入主题（可选）**
   - 输入播客主题，系统会根据主题生成对话
   - 如果留空，系统会根据角色人设自由生成对话

3. **生成播客**
   - 点击"生成播客"按钮
   - 系统会：
     1. 调用混元模型生成符合角色人设的对话文本
     2. 使用IndexTTS-2生成各角色的音频
     3. 合成完整的播客音频

### 💡 主题深度播客（子题目3）

**功能**：基于指定主题生成有深度、引发思考的播客音频

**步骤：**

1. **输入主题**
   - 在"播客主题"文本框中输入想要讨论的主题
   - 例如："人工智能对人类社会的影响"

2. **设置参数**
   - 选择角色数量（2-3个）
   - 选择深度级别（深度/中等/浅层）

3. **上传音色文件**
   - 为每个角色上传音色参考音频

4. **生成播客**
   - 点击"生成播客"按钮
   - 系统会：
     1. 调用混元模型生成深度对话内容
     2. 使用IndexTTS-2生成音频
     3. 合成完整的播客音频

## 快速测试

### 使用示例音频文件

可以使用 IndexTTS-2 自带的示例音频：

```powershell
# 示例音频文件位置
index-tts/examples/voice_01.wav
index-tts/examples/voice_02.wav
index-tts/examples/voice_03.wav
```

### 快速测试步骤

1. 打开 WebUI：http://localhost:7861
2. 选择"多角色互动播客"标签页
3. 使用默认的示例文本（已预填）
4. 上传示例音频文件：
   - 角色A音色：`index-tts/examples/voice_01.wav`
   - 角色B音色：`index-tts/examples/voice_02.wav`
5. 点击"生成播客"

## 常见问题

### Q1: 点击生成后没有反应

**可能原因：**
- 正在生成中，请耐心等待（生成可能需要几分钟）
- 查看浏览器控制台是否有错误信息
- 检查服务器终端是否有错误输出

**解决方案：**
- 等待一段时间（首次生成可能需要更长时间）
- 检查音色文件是否正确上传
- 检查文本格式是否正确

### Q2: 生成失败，显示错误信息

**常见错误：**

1. **"文本中未找到角色标记"**
   - 确保文本使用了 `[角色名]` 格式
   - 参考界面中的示例格式

2. **"缺少音色文件"**
   - 确保为每个角色上传了音色文件
   - 检查文件格式是否为WAV

3. **"API调用失败"**
   - 检查网络连接
   - 检查API密钥是否有效

### Q3: 生成速度很慢

**原因：**
- 首次生成需要下载额外模型文件
- 音频生成本身需要时间
- 如果使用CPU模式会更慢

**优化建议：**
- 使用GPU加速（如果可用）
- 启用FP16模式（减少显存占用）
- 使用CUDA内核加速

### Q4: 生成的音频质量不好

**可能原因：**
- 音色参考音频质量不佳
- 文本过长导致分句问题
- 参数设置不当

**解决方案：**
- 使用清晰、无噪音的音色参考音频
- 适当调整"分句最大Token数"参数
- 尝试不同的音色文件

## 输出文件位置

生成的播客音频保存在：
```
outputs/podcasts/podcast_<时间戳>.wav
```

## 性能优化建议

### 如果显存充足（16GB+）

在启动时使用优化参数：

```powershell
cd index-tts
uv run python ../run_podcast_webui.py --fp16 --cuda_kernel
```

### 如果显存有限（8GB）

```powershell
cd index-tts
uv run python ../run_podcast_webui.py --fp16
```

## 技术支持

如有问题，请查看：
- [README_PODCAST.md](README_PODCAST.md) - 详细文档
- [QUICKSTART.md](QUICKSTART.md) - 快速开始
- [INSTALL.md](INSTALL.md) - 安装指南






