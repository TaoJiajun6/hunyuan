# 快速同步到目标仓库的命令

目标仓库地址：`https://openatom.tech/tencent-hunyuan-ai-podcast/edd7c1ae12ad8aaee84c9f1e4bf473a0.git`

## 方法：使用 PowerShell 脚本（推荐）

已为您创建了自动化脚本 `sync_to_target.ps1`，直接运行：

```powershell
.\sync_to_target.ps1 -TargetRepoUrl "https://openatom.tech/tencent-hunyuan-ai-podcast/edd7c1ae12ad8aaee84c9f1e4bf473a0.git" -CommitMessage "优化鸿蒙端UI，更新文档"
```

## 方法二：手动步骤

如果脚本有问题，可以手动执行以下步骤：

### 1. 先提交本地更改（如果还没提交）

```powershell
# 如果提交信息有中文编码问题，可以使用：
git add .
git commit -m "optimize harmonyos UI and update docs"
```

### 2. 克隆目标仓库到临时目录

```powershell
cd ..
git clone https://openatom.tech/tencent-hunyuan-ai-podcast/edd7c1ae12ad8aaee84c9f1e4bf473a0.git temp-target-repo
cd temp-target-repo
```

### 3. 切换到 master 分支

```powershell
git checkout master
```

### 4. 删除旧的 hunyuan 文件夹

```powershell
git rm -r hunyuan
# 或者如果已经删除了
Remove-Item -Recurse -Force hunyuan -ErrorAction SilentlyContinue
```

### 5. 复制本地所有文件到目标仓库

```powershell
# 回到父目录
cd ..

# 使用 robocopy 复制（排除 .git、__pycache__ 等）
robocopy hunyuan\hunyuan temp-target-repo\hunyuan /E /XD .git __pycache__ .cursor .idea node_modules /XF .gitignore
```

### 6. 进入目标仓库，提交并推送

```powershell
cd temp-target-repo
git add .
git commit -m "优化鸿蒙端UI，更新文档"
git push origin master
```

### 7. 清理临时目录（可选）

```powershell
cd ..
Remove-Item -Recurse -Force temp-target-repo
```

## 注意事项

1. **确保有推送权限**：目标仓库需要有写权限
2. **备份**：同步前建议先备份目标仓库
3. **排除文件**：确保 `.env`、`__pycache__` 等文件不会被推送
4. **大文件**：模型文件可能很大，如果目标仓库有大小限制，可能需要排除

## 如果遇到问题

- **权限错误**：检查是否有目标仓库的推送权限
- **冲突**：如果目标仓库有新的更改，可能需要先 pull 再 push
- **编码问题**：如果提交信息有中文乱码，使用英文提交信息

