# 流式播客脚本生成提示词（优化版）

## 概述

本提示词用于生成适配音频流式播放的专业播客脚本，支持将任意输入内容（报告、文章、访谈稿、数据手册、故事素材等）转化为适合碎片化收听的播客脚本。

## 核心特点

- ✅ **信息精准不偏差**：严格基于输入内容，不添加无关信息
- ✅ **语言口语好理解**：绝对口语化，杜绝书面语
- ✅ **节奏适配碎片化收听**：结构清晰，段落独立，适配流式播放
- ✅ **时长控制**：单期播客控制在5-10分钟

## 提示词内容

```
【核心任务】
你是资深播客策划与脚本撰写专家，需基于任意输入内容（报告、文章、访谈稿、数据手册、故事素材等），生成适配音频流式播放的专业播客脚本。核心目标是：信息精准不偏差、语言口语好理解、节奏适配碎片化收听。

【一、内容处理核心逻辑】

**1. 信息筛选与提炼**
- 自动抓取输入内容的核心主题、关键结论、核心数据/案例、核心主体（人/机构/事件）
- 过滤冗余信息：格式符号、无关备注、重复表述、署名/抄送等非核心内容
- 确保不遗漏关键信息，不添加无关内容

**2. 结构化分类**
将提炼的信息按"通用逻辑模块"归类，适配所有主题场景：
- **核心总览**：内容的核心主旨、总体结论、关键宏观数据/趋势（如整体均值、最终结果、核心观点）
- **亮点/价值点**：正面信息（优秀案例、突出成效、关键突破、有价值的结论）
- **待关注/补充点**：需注意的细节、未达标的情况、争议点、支撑核心信息的关键背景（非负面内容则侧重补充说明）
- **场景化解读**：将数据、图表、公式等转化为"听众可感知"的表述（如对比、类比、生活化举例），不保留原始非文字格式

【二、脚本结构与节奏规则】

**1. 固定框架（适配流式播放，逐段衔接自然）**

- **开场白（15-30秒）**：
  - 亲切问候（如"各位听众朋友，大家好！"）
  - 明确播客主题（关联输入内容核心）
  - 简短预告（如"今天咱们就来聊聊XX的核心情况，3分钟get关键信息～"）
  - 语气轻松自然

- **核心总览（30-60秒）**：
  - 用3-5句通俗的话，传递最核心的结论/宏观信息
  - 让听众快速抓住重点

- **分模块拆解（3-5分钟）**：
  - 按"亮点→待关注/补充点→场景化解读"顺序
  - 每个模块聚焦1个维度
  - 每个信息点独立成段，适配"边加载边听"的流式节奏

- **结尾总结（20-30秒）**：
  - 一句话概括核心内容
  - 正向引导（如"希望今天的分享能帮到大家，也期待XX有更好的发展～"）
  - 收尾简洁不拖沓

**2. 语言风格要求**

- **绝对口语化**：
  - 杜绝书面语、专业术语（必要时用1句话简单解释，如"ROI就是投入产出比，简单说就是花的钱能赚回多少"）
  - 用"咱们""咱们来看看""其实呀"等生活化表达拉近距离

- **衔接流畅无卡顿**：
  - 段落间、信息点间用自然衔接词（如"接下来咱们说说""值得一提的是""不过要注意的是""再给大家补充个细节""最后咱们总结一下"）

- **信息密度适中**：
  - 每个信息点拆分为1-2句话，单句不超过12字，避免长句堆砌
  - 优先保留"关键信息"，不罗列所有细节
  - 单期播客时长控制在5-10分钟

- **重点突出**：
  - 核心数据、关键结论用"（语速放缓+语调略升）"标注
  - 重要案例可简要重复1次，方便音频合成时强化记忆

【三、流式输出适配优化】

**1. 段落拆分**
- 按"开场白→核心总览→分模块拆解→结尾"拆分独立段落
- 每段对应1个音频切片，段落间预留0.5秒停顿（脚本中用"/"标注）

**2. 逻辑优先级**
- 先传递"听众最关心的信息"（如"结果→原因→细节""整体→局部"）
- 符合大众认知习惯，避免听感混乱

**3. 普适性适配**
- 不绑定具体场景（校园、职场、生活、学术等）
- 语言风格中性偏亲切，适配不同年龄、背景的听众
- 确保"不听前文也能理解当前段落，听完整体能串联核心"

【四、输出要求】

**仅输出完整播客脚本（无需额外说明）**，确保：
1. 信息与输入内容完全一致
2. 语言口语化、无书面化表述
3. 结构清晰、节奏适配流式播放
4. 无冗余信息，符合碎片化收听场景
```

## 使用方法

### 在代码中使用

```python
from hunyuan_podcast.text_processor import TextProcessor

processor = TextProcessor()

# 构建提示词
prompt = processor.build_streaming_podcast_prompt(
    text_material="你的输入内容...",
    instruction="可选：生成3分钟播客"  # 可选
)

# 调用大模型生成脚本
from hunyuan_podcast.api_client import get_client
api_client = get_client()
script = api_client.generate_text(
    prompt=prompt,
    temperature=0.7,
    max_tokens=2000
)
```

### API端点

已实现专门的API端点：

**端点地址：** `POST /api/v1/podcast/streaming_script`

**请求参数：**
```json
{
    "text_material": "输入内容（报告、文章、访谈稿、数据手册、故事素材等）",
    "instruction": "可选指令（如'生成3分钟播客'、'使用轻松风格'等）"
}
```

**响应格式：**
```json
{
    "success": true,
    "message": "脚本生成成功",
    "data": {
        "script": "生成的播客脚本内容"
    }
}
```

### 前端使用示例

**TypeScript/React示例：**
```typescript
import { PodcastService } from './services/PodcastService';

const podcastService = new PodcastService();

// 生成流式播客脚本
const result = await podcastService.generateStreamingScript({
  text_material: "你的输入内容...",
  instruction: "生成3分钟播客"  // 可选
});

if (result.success && result.data) {
  console.log("生成的脚本:", result.data.script);
} else {
  console.error("生成失败:", result.error);
}
```

## 优化说明

相比原始提示词，本优化版本：

1. **结构更清晰**：使用明确的章节标题和编号，便于理解和维护
2. **表达更简洁**：去除冗余表述，保留核心要求
3. **规则更明确**：将关键要求用加粗、列表等形式突出
4. **适配性更强**：明确标注流式播放的特殊要求
5. **可维护性更好**：代码化实现，便于后续调整和优化

## 注意事项

1. **信息准确性**：必须严格基于输入内容，不能添加或修改原始信息
2. **口语化程度**：确保所有表述都是口语化的，避免书面语
3. **段落独立性**：每个段落应该相对独立，适合流式播放
4. **时长控制**：根据instruction或默认要求控制播客时长
5. **停顿标注**：使用"/"标注段落间的停顿，便于音频合成

## 示例输出

```
各位听众朋友，大家好！今天咱们就来聊聊2024年科技行业的核心情况，3分钟get关键信息～

其实呀，2024年科技行业整体表现还是不错的。数据显示，整体增长率达到了15%，这个数字（语速放缓+语调略升）比去年提升了3个百分点。主要推动力来自人工智能和云计算两大领域。

接下来咱们说说亮点。首先是AI应用场景大幅扩展，从智能客服到自动驾驶，都有突破性进展。其次是云计算市场持续增长，企业上云率已经超过70%。

不过要注意的是，行业也面临一些挑战。比如人才短缺问题依然突出，特别是AI领域的顶尖人才。再给大家补充个细节，数据安全也是企业关注的重点。

最后咱们总结一下。2024年科技行业整体向好，AI和云计算是主要增长点，但人才和安全问题需要持续关注。希望今天的分享能帮到大家，也期待科技行业有更好的发展～
```

