# AI播客生成系统使用说明

## 目录

- [系统概述](#系统概述)
- [快速开始](#快速开始)
- [功能详解](#功能详解)
- [使用示例](#使用示例)
- [常见问题](#常见问题)
- [故障排查](#故障排查)

## 系统概述

基于腾讯混元大模型(Hunyuan-A13B-Instruct)和SoulX-Podcast的AI播客生成系统支持三种播客生成模式:

1. **多角色互动播客**: 将文本素材转化为多角色自然互动的播客音频
   - 支持多种输入类型: 文字、文件、公众号文章、网页URL、PDF、Word文档
   - 自动识别角色并生成对应的播客音频
   - 支持情绪标注，增强对话表现力
   - 智能数字转中文：自动将数字、日期、价格、技术术语转换为中文读音

2. **自定义角色人设播客**: 根据角色人设生成符合风格的播客对话
   - 支持为每个角色设置详细的人设描述(身份、性格、说话风格、口头禅等)
   - 支持最多4个角色同时参与对话
   - 确保角色人设一致性，风格固化

3. **主题深度播客**: 基于指定主题生成有深度、引发思考的播客内容
   - 支持多维度分析，提供全面视角
   - 引用理论、数据、案例等支撑观点
   - 支持深度级别选择(深度/中等/浅层)
   - 支持1-3个角色参与讨论

### 核心功能特性

- ✅ **智能背景音乐**: AI自动分析播客内容，从云存储或本地选择合适背景音乐
- ✅ **批量生成**: 支持一次提交最多50个播客生成任务，自动队列管理
- ✅ **文本分析**: 自动分析文本素材，提取主题、角色、情绪等信息
- ✅ **进度跟踪**: 实时进度查询，支持SSE流式进度推送
- ✅ **云存储集成**: 支持华为AGC云存储，自动上传和管理音频文件
- ✅ **云数据库集成**: 支持华为AGC CloudDB，自动保存播客元数据
- ✅ **任务管理**: 完整的任务队列、状态查询、历史记录功能

系统提供四种使用方式:

1. **Web UI界面**: 适合测试和演示，基于Gradio构建
2. **REST API**: 适合工作流集成，提供完整的RESTful API
3. **编程接口**: 适合开发者集成到其他应用
4. **鸿蒙移动应用**: 适合移动端使用，完整的移动端体验

## 快速开始

### 1. 环境准备

#### 系统要求

- Python 3.10+
- Git 和 Git-LFS
- CUDA Toolkit 12.8+ (如果使用GPU,推荐)

#### 安装依赖

**使用conda环境(推荐)**

```bash
# 1. 克隆仓库
git clone https://github.com/your-username/hunyuan-podcast.git
cd hunyuan

# 2. 创建conda环境
conda create -n hunyuan python=3.10

# 3. 激活环境
conda activate hunyuan

# 4. 安装依赖
pip install -r requirements_podcast.txt
```

**注意**: 以后每次使用系统前，都需要先激活conda环境: `conda activate hunyuan`

#### 下载模型

**下载SoulX-Podcast模型:**

```bash
# 使用HuggingFace CLI
pip install -U huggingface_hub
huggingface-cli download --resume-download Soul-AILab/SoulX-Podcast-1.7B --local-dir SoulX-Podcast/pretrained_models/SoulX-Podcast-1.7B

# 或使用Python
python -c "from huggingface_hub import snapshot_download; snapshot_download('Soul-AILab/SoulX-Podcast-1.7B', local_dir='SoulX-Podcast/pretrained_models/SoulX-Podcast-1.7B')"
```

**配置模型路径:**

模型路径会自动检测，如需手动配置，可在`hunyuan_podcast/config.py`中设置:

```python
SOULX_PODCAST_MODEL_DIR = "SoulX-Podcast/pretrained_models/SoulX-Podcast-1.7B"
```

#### 配置API密钥

**方式一: 使用环境变量(推荐)**

创建`.env`文件(在项目根目录):

```bash
# 混元大模型API配置
HUNYUAN_API_KEY=your-api-key  # 从 https://console.cloud.tencent.com/hunyuan/start 获取
HUNYUAN_MODEL=hunyuan-a13b  # 可选，默认值
HUNYUAN_FAST_THINKING=True  # 可选，默认开启快思考模式

# 华为AGC云存储配置(可选)
AGC_STORAGE_URL=https://ops-server-drcn.agcstorage.link/v0/
AGC_BUCKET=your-bucket-name
AGC_CLIENT_ID=your-client-id
AGC_CLIENT_SECRET=your-client-secret
AGC_PRODUCT_ID=your-product-id

# 华为AGC云数据库配置(可选)
AGC_API_KEY=your-api-key
AGC_CLOUD_DB_ZONE=cloudDBZone
```

**方式二: 直接在config.py中配置**

在`hunyuan_podcast/config.py`中直接设置:

```python
HUNYUAN_API_KEY = "your-api-key"  # 从 https://console.cloud.tencent.com/hunyuan/start 获取
```

**注意**: API密钥需要从腾讯云混元控制台获取，访问: https://console.cloud.tencent.com/hunyuan/start

#### 配置HuggingFace镜像(可选，推荐)

如果网络访问HuggingFace较慢，建议设置镜像:

```bash
# Linux/macOS
export HF_ENDPOINT="https://hf-mirror.com"

# Windows PowerShell
$env:HF_ENDPOINT="https://hf-mirror.com"

# Windows CMD
set HF_ENDPOINT=https://hf-mirror.com
```

系统启动脚本会自动设置此镜像，如果已经手动设置，则使用手动设置的值。

### 2. 启动服务

#### 启动Web UI

```bash
python run_podcast_webui.py
```

访问: `http://localhost:7861`

#### 启动API服务

```bash
# 基本启动（自动检测GPU并启用优化）
python run_api_server.py --host 0.0.0.0 --port 8000

# 启用多进程模式（支持并发处理）
python run_api_server.py --host 0.0.0.0 --port 8000 --workers 2

# 禁用FP16（如果GPU不支持）
python run_api_server.py --host 0.0.0.0 --port 8000 --no-fp16

# 禁用CUDA内核加速
python run_api_server.py --host 0.0.0.0 --port 8000 --no-cuda-kernel
```

**启动参数说明**:
- `--host`: 服务主机地址，默认0.0.0.0(允许外部访问)
- `--port`: 服务端口，默认8000
- `--fp16`: 启用FP16精度（GPU加速，自动检测GPU时默认启用）
- `--no-fp16`: 禁用FP16精度
- `--cuda_kernel`: 启用CUDA内核加速（GPU加速，自动检测GPU时默认启用）
- `--no-cuda-kernel`: 禁用CUDA内核加速
- `--workers`: 工作进程数，>=2时可并发处理进度查询

**访问地址**:
- API文档: `http://localhost:8000/docs`
- 健康检查: `http://localhost:8000/health`
- 进度查询: `GET /api/v1/podcast/progress/{job_id}`
- 进度流式推送: `GET /api/v1/podcast/progress/{job_id}/stream`

### 3. 使用系统

#### 使用Web UI

1. 打开浏览器访问 `http://localhost:7861`
2. 选择播客生成功能
3. 输入文本、选择音色等
4. 点击生成按钮
5. 等待生成完成
6. 下载生成的播客音频

#### 使用API

**基本示例 - 多角色播客**:

```bash
curl -X POST "http://localhost:8000/api/v1/podcast/multi_role" \
  -H "Content-Type: application/json" \
  -d '{
    "text": "[角色A]你好 [角色B]你好啊",
    "role_voice_urls": {
      "角色A": "https://your-cloud-storage.com/voices/voice_a.wav",
      "角色B": "https://your-cloud-storage.com/voices/voice_b.wav"
    },
    "job_id": "my_job_123"
  }'
```

**使用进度查询**:

```bash
# 查询进度
curl "http://localhost:8000/api/v1/podcast/progress/my_job_123"

# 流式进度推送（SSE）
curl "http://localhost:8000/api/v1/podcast/progress/my_job_123/stream"
```

**注意**: 
- 音色文件必须先上传到云存储，获取URL后使用。可以使用 `/api/v1/podcast/upload_voice` 接口上传音色文件。
- 建议使用`job_id`参数以便查询生成进度。
- 支持多种输入类型：文字、文件URL、公众号文章URL、网页URL等。

## 功能详解

### 1. 多角色互动播客生成

#### 功能说明

将用户提供的文本素材转化为多角色自然互动的播客音频。支持多种输入方式，包括文字、文件、公众号文章、网页URL等。

#### 使用步骤

1. **输入内容**: 可选择以下方式之一输入:
   - **文字输入**: 直接输入包含角色标记的文本
   - **文件上传**: 上传文本文件(.txt)或Word文档(.doc/.docx)的云存储URL
   - **公众号文章**: 提供公众号文章URL，系统自动提取内容
   - **网页内容**: 提供网页URL，系统自动提取内容
   - **PDF文件**: 提供PDF文件的云存储URL，系统自动提取文本

2. **选择音色**: 为每个角色提供音色参考音频的云存储URL
   - 必须先上传音色文件到云存储，获取URL后使用
   - 可以使用 `/api/v1/podcast/upload_voice` API上传音色文件

3. **设置参数** (可选):
   - 播客名称
   - 主题
   - 指令内容(如"生成5分钟播客"、"使用轻松风格"等)
   - 静音间隔(角色切换时的静音间隔，默认800ms)

4. **生成播客**: 提交请求，系统会自动生成播客音频

5. **查询进度**: 使用`job_id`查询生成进度，支持轮询或SSE流式推送

6. **下载音频**: 生成完成后下载播客音频

#### 文本格式要求

- **角色标记**: 使用`[角色名]`格式标记角色，例如`[角色A]你好`
- **情绪标注**: 支持`[角色名](情绪地):内容`格式，例如`[角色A](兴奋地):太好了!`
- **多行文本**: 支持多行文本，每行一个角色的发言
- **普通文本**: 如果不包含角色标记，系统会自动识别并分配角色

#### 输入类型说明

支持以下输入类型（通过`input_type`参数指定）:

- `文字`: 直接文字输入
- `文字+指令`: 文字输入 + 指令内容
- `文件`: 文本文件或Word文档URL
- `文件+指令`: 文件URL + 指令内容
- `公众号`: 公众号文章URL
- `公众号+指令`: 公众号文章URL + 指令内容
- `网页`: 网页URL
- `网页+指令`: 网页URL + 指令内容
- `文字+英文指令`: 文字输入 + 英文指令

#### 音色文件要求

- **格式**: WAV格式(推荐)，也支持MP3等常见格式
- **时长**: 5-30秒（推荐20-30秒）
- **大小**: 最大50MB（服务器端限制）
- **质量**: 清晰、无噪音的音频文件效果更好
- **上传方式**: 必须先上传到云存储，获取URL后使用
  - 使用 `/api/v1/podcast/upload_voice` API上传
  - 或通过鸿蒙应用上传到华为AGC云存储

### 2. 自定义角色人设播客生成

#### 功能说明

根据用户为每个角色设置的人设描述,生成符合风格的播客对话。系统会根据角色人设生成符合风格的对话，确保角色人设一致性。

#### 使用步骤

1. **设置角色人设**: 为每个角色设置(至少2个，最多4个角色):
   - **角色名称** (必需): 角色的名称
   - **音色URL** (必需): 音色文件云存储URL
   - **身份/职业** (可选): 角色的身份或职业
   - **核心性格** (可选): 角色的核心性格特征
   - **口头禅/说话习惯** (可选): 角色的口头禅或说话习惯
   - **说话风格** (可选): 角色的说话风格
   - **与其他角色的关系** (可选): 与其他角色的关系

2. **输入文本素材** (必需): 提供播客的文本素材

3. **设置其他参数** (可选):
   - **主题**: 播客主题(主要用于背景音乐选择)
   - **指令内容**: 控制播客生成过程，如"生成1分钟播客"、"使用轻松风格"等
   - **分类**: 播客分类，用于背景音乐选择
   - **静音间隔**: 角色切换静音间隔（毫秒），默认800ms
   - **背景音乐音量**: 背景音乐音量（0.0-1.0），默认0.3

4. **生成播客**: 提交请求，系统会根据角色人设生成符合风格的播客音频

5. **查询进度**: 使用`job_id`查询生成进度

6. **下载音频**: 生成完成后下载播客音频

#### 角色人设示例

- **角色A**:
  - 身份/职业: 科技博主
  - 核心性格: 外向幽默
  - 说话风格: 语速较快,常用网络流行语
  - 音色: 男声普通话

- **角色B**:
  - 身份/职业: 技术专家
  - 核心性格: 理性严谨
  - 说话风格: 语速平稳,逻辑性强
  - 音色: 男声普通话

#### 人设要求

- **详细描述**: 人设描述越详细,生成的内容越符合要求
- **风格一致**: 确保角色人设风格一致,避免冲突
- **音色匹配**: 选择符合角色人设的音色

### 3. 主题深度播客生成

#### 功能说明

基于用户指定的主题,生成有深度、引发思考的播客内容。系统会从多个维度分析主题，引用理论、数据、案例等支撑观点。

#### 使用步骤

1. **输入主题**: 输入播客主题，例如"AI与人类的关系"

2. **设置角色**: 
   - **角色数量**: 选择1-3个角色
   - **音色URL**: 为每个角色提供音色文件的云存储URL

3. **选择深度级别**: 
   - **深度**: 从多个维度分析主题，引用理论、数据、案例等，使用开放式结尾
   - **中等**: 从3-4个维度分析主题，引用相关的理论、案例或研究，可以有一定结论
   - **浅层**: 从2-3个维度分析主题，可以提及相关的例子或观点，可以有明确的结论

4. **设置其他参数** (可选):
   - **指令内容**: 控制播客生成过程，如"生成1分钟播客"
   - **分类**: 播客分类，用于背景音乐选择
   - **静音间隔**: 角色切换静音间隔（毫秒），默认800ms
   - **背景音乐音量**: 背景音乐音量（0.0-1.0），默认0.3

5. **生成播客**: 提交请求，系统会自动生成有深度的播客内容

6. **查询进度**: 使用`job_id`查询生成进度

7. **下载音频**: 生成完成后下载播客音频

#### 深度级别说明

- **深度**: 从多个维度分析主题,引用理论、数据、案例等,使用开放式结尾
- **中等**: 从3-4个维度分析主题,引用相关的理论、案例或研究,可以有一定结论
- **浅层**: 从2-3个维度分析主题,可以提及相关的例子或观点,可以有明确的结论

#### 主题要求

- **明确具体**: 主题要明确具体,避免过于宽泛
- **有深度**: 主题要有一定的深度,适合深度讨论
- **有争议**: 主题最好有一定的争议性,便于多角度讨论

### 4. 批量生成

#### 功能说明

支持一次提交最多50个播客生成任务，系统会自动队列管理，按顺序处理。

#### 使用步骤

1. **准备任务列表**: 准备最多50个播客生成任务
2. **提交批量请求**: 使用 `/api/v1/podcast/batch` 接口提交批量任务
3. **查询任务状态**: 使用 `/api/v1/podcast/task/{task_id}` 查询每个任务的状态
4. **获取结果**: 任务完成后，从响应中获取生成的播客音频

#### 注意事项

- 批量任务会自动排队处理，避免GPU内存溢出
- 每个任务都有独立的`task_id`，可以单独查询状态
- 建议批量任务数量不超过50个，以确保系统稳定性

### 5. 文本分析

#### 功能说明

自动分析文本素材，提取主题、角色、情绪等信息，帮助用户更好地准备播客内容。

#### 使用步骤

1. **提交文本**: 使用 `/api/v1/podcast/analyze` 接口提交要分析的文本
2. **获取分析结果**: 系统返回主题、角色、情绪等分析结果
3. **使用分析结果**: 根据分析结果调整播客生成参数

### 6. 音色文件管理

#### 上传音色文件

使用 `/api/v1/podcast/upload_voice` 接口上传音色文件到云存储:

```bash
curl -X POST "http://localhost:8000/api/v1/podcast/upload_voice" \
  -H "Content-Type: application/json" \
  -d '{
    "base64_data": "base64编码的音频数据",
    "filename": "voice_a.wav"
  }'
```

#### 查询已上传的音色

使用 `/api/v1/podcast/voices` 接口查询已上传的音色文件列表。

## API参考

### 主要API端点

#### 1. 多角色互动播客

**端点**: `POST /api/v1/podcast/multi_role`

**请求参数**:
```json
{
  "text": "播客文本（可选，如果使用text_file_url或input_url）",
  "text_file_url": "文本文件云存储URL（可选，支持单个URL或URL数组）",
  "input_type": "输入类型（可选）：文字、文字+指令、公众号、公众号+指令、网页、网页+指令、文件、文件+指令、文字+英文指令",
  "input_url": "输入URL（可选，用于公众号、网页、PDF等）",
  "instruction": "指令内容（可选）",
  "role_voice_urls": {
    "角色A": "音色文件云存储URL",
    "角色B": "音色文件云存储URL"
  },
  "silence_interval": 800,
  "podcast_name": "播客名称（可选）",
  "topic": "主题（可选）",
  "job_id": "任务ID（可选）"
}
```

#### 2. 自定义角色播客

**端点**: `POST /api/v1/podcast/character`

**请求参数**:
```json
{
  "characters": [
    {
      "name": "角色名称",
      "voice_url": "音色文件云存储URL",
      "identity": "身份/职业（可选）",
      "personality": "核心性格（可选）",
      "catchphrase": "口头禅（可选）",
      "speaking_style": "说话风格（可选）",
      "relationship": "与其他角色的关系（可选）"
    }
  ],
  "text": "文本素材（必需）",
  "topic": "主题（可选）",
  "instruction": "指令内容（可选）",
  "category": "分类（可选）",
  "silence_interval": 800,
  "background_volume": 0.3,
  "job_id": "任务ID（可选）"
}
```

#### 3. 主题深度播客

**端点**: `POST /api/v1/podcast/deep`

**请求参数**:
```json
{
  "topic": "播客主题",
  "role_voice_urls": {
    "角色A": "音色文件云存储URL",
    "角色B": "音色文件云存储URL"
  },
  "num_characters": 2,
  "depth_level": "深度|中等|浅层",
  "instruction": "指令内容（可选）",
  "category": "分类（可选）",
  "silence_interval": 800,
  "background_volume": 0.3,
  "job_id": "任务ID（可选）"
}
```

#### 4. 批量生成

**端点**: `POST /api/v1/podcast/batch`

**请求参数**:
```json
{
  "tasks": [
    {
      "type": "multi_role|character|deep",
      "params": { /* 对应类型的参数 */ }
    }
  ]
}
```

#### 5. 文本分析

**端点**: `POST /api/v1/podcast/analyze`

**请求参数**:
```json
{
  "text": "要分析的文本"
}
```

#### 6. 上传音色文件

**端点**: `POST /api/v1/podcast/upload_voice`

**请求参数**:
```json
{
  "base64_data": "base64编码的音频文件",
  "filename": "文件名（可选）"
}
```

#### 7. 查询进度

**端点**: `GET /api/v1/podcast/progress/{job_id}`

**响应**:
```json
{
  "success": true,
  "data": {
    "progress": 50,
    "status": "processing",
    "message": "正在生成音频...",
    "done": false
  }
}
```

#### 8. 流式进度推送

**端点**: `GET /api/v1/podcast/progress/{job_id}/stream`

**说明**: 使用Server-Sent Events (SSE)推送实时进度更新

#### 9. 查询任务状态

**端点**: `GET /api/v1/podcast/task/{task_id}`

#### 10. 查询历史记录

**端点**: `GET /api/v1/podcast/history`

**查询参数**: `?limit=10&offset=0`

## 使用示例

### 示例1: 多角色互动播客

#### 输入文本

```
[角色A]大家好,欢迎收听本期播客!我是主持人小明。
[角色B]大家好,我是嘉宾小红。今天我们要聊什么话题呢?
[角色A]今天我们来聊聊AI技术的发展。
[角色B]这个话题很有意思!AI技术最近发展得很快。
[角色A]没错!从ChatGPT到现在的各种AI应用,AI技术正在改变我们的生活。
[角色B]是的,AI技术确实给我们带来了很多便利,但也引发了一些担忧。
[角色A]比如说?
[角色B]比如说AI可能会取代人类的工作,或者AI可能会产生偏见。
[角色A]这些担忧确实存在,但我们也要看到AI技术的积极一面。
[角色B]没错,AI技术可以帮助我们解决很多问题,提高效率。
[角色A]好了,今天关于AI技术的讨论就到这里。
[角色B]感谢大家的收听!
```

#### 生成效果

- 自动识别两个角色(角色A和角色B)
- 为每个角色生成对应的语音
- 合成为完整的播客音频
- 添加角色切换时的静音间隔
- 支持添加背景音乐

### 示例2: 自定义角色人设播客

#### 输入角色人设

- **角色A**:
  - 身份/职业: 科技博主
  - 核心性格: 外向幽默
  - 说话风格: 语速较快,常用网络流行语
  - 音色: 男声普通话

- **角色B**:
  - 身份/职业: 技术专家
  - 核心性格: 理性严谨
  - 说话风格: 语速平稳,逻辑性强
  - 音色: 男声普通话

#### 输入主题

"AI技术的发展"

#### 生成效果

- 根据角色人设生成符合风格的对话
- 角色A使用网络流行语,语速较快
- 角色B使用专业术语,逻辑性强
- 确保角色人设一致性
- 生成自然流畅的播客音频

### 示例3: 主题深度播客

#### 输入主题

"AI与人类的关系"

#### 选择参数

- 角色数量: 2个
- 深度级别: 深度

#### 生成效果

- 从多个维度分析主题(历史维度、哲学视角、科学前沿、人文视角等)
- 引用理论、数据、案例等支撑观点
- 包含对立观点,体现思维的辩证性
- 使用开放式结尾,引导听众继续思考
- 生成有深度、有见地的播客内容

## 常见问题

### Q1: 如何获取API密钥?

A: 访问 https://console.cloud.tencent.com/hunyuan/start 注册腾讯云账号,开通混元大模型服务,获取API密钥。

### Q2: 模型文件在哪里下载?

A: 使用HuggingFace CLI下载:
```bash
huggingface-cli download --resume-download Soul-AILab/SoulX-Podcast-1.7B --local-dir SoulX-Podcast/pretrained_models/SoulX-Podcast-1.7B
```

### Q3: 音色文件有什么要求?

A: 
- 格式: WAV格式(推荐)
- 时长: 5-30秒
- 大小: 最大10MB(客户端)或50MB(服务器)
- 质量: 清晰、无噪音的音频文件效果更好

### Q4: 生成播客需要多长时间?

A: 
- 文本生成: 5-30秒(取决于文本长度和复杂度)
- 语音合成: 10-60秒(取决于音频长度)
- 背景音乐选择: 2-5秒
- 总生成时间: 20-90秒(单角色), 30-120秒(多角色)
- 使用GPU可以显著加速生成速度

### Q5: 支持哪些音色格式?

A: 支持WAV、MP3等常见音频格式,推荐使用WAV格式。

### Q6: 如何提高生成质量?

A: 
- 使用清晰、无噪音的音色文件
- 提供详细的角色人设描述
- 使用明确的主题描述
- 确保文本格式正确

### Q7: 支持哪些语言?

A: 目前主要支持中文(普通话),未来可能支持更多语言。

### Q8: 如何添加背景音乐?

A: 
- 系统会自动根据播客内容、主题、分类等智能选择背景音乐
- 在API请求中可以设置`category`参数帮助系统选择更合适的背景音乐
- 可以通过`background_volume`参数调整背景音乐音量(0.0-1.0)
- 背景音乐会自动混合到播客音频中

### Q9: 如何调整生成参数?

A: 在`hunyuan_podcast/config.py`中可以调整温度、最大token数等参数。

### Q10: 如何部署到云端?

A: 参考[部署说明.md](部署说明.md)进行部署。系统支持Cloud Studio云端部署。

### Q11: 如何查询生成进度?

A: 
- 在API请求中提供`job_id`参数
- 使用 `GET /api/v1/podcast/progress/{job_id}` 查询进度
- 或使用 `GET /api/v1/podcast/progress/{job_id}/stream` 获取SSE流式进度推送
- 进度包含百分比、当前状态、消息等信息

### Q12: 支持哪些输入类型?

A: 
- **文字**: 直接文字输入
- **文件**: 文本文件(.txt)或Word文档(.doc/.docx)的云存储URL
- **公众号文章**: 公众号文章URL，系统自动提取内容
- **网页**: 网页URL，系统自动提取内容
- **PDF**: PDF文件的云存储URL，系统自动提取文本
- 可以通过`input_type`参数指定输入类型

### Q13: 音色文件如何上传?

A: 
- 使用 `/api/v1/podcast/upload_voice` API上传音色文件
- 或通过鸿蒙应用上传到华为AGC云存储
- 上传后获取云存储URL，在API请求中使用`role_voice_urls`或`voice_url`参数
- 不再支持base64编码方式（避免请求体过大）

## 故障排查

### 问题1: 模型加载失败

**症状**: 启动时提示模型加载失败

**解决方案**:
1. 检查模型文件是否已下载
2. 检查模型路径配置是否正确
3. 检查CUDA是否可用(如果使用GPU)
4. 检查依赖是否已安装

### 问题2: API调用失败

**症状**: API调用时返回错误

**解决方案**:
1. 检查API密钥是否有效（腾讯云混元API密钥）
2. 检查网络连接是否正常
3. 检查API调用额度是否充足
4. 检查API服务器地址是否正确
5. 检查请求参数格式是否正确（特别是`role_voice_urls`必须是云存储URL）
6. 查看API返回的错误信息，根据错误提示进行修复

### 问题3: 音频生成失败

**症状**: 音频生成时返回错误

**解决方案**:
1. 检查音色文件URL是否有效（必须是可访问的云存储URL）
2. 检查音色文件格式是否正确（推荐WAV格式）
3. 检查音色文件大小是否超出限制（最大50MB）
4. 检查文本格式是否正确
5. 检查模型是否已加载
6. 检查GPU内存是否充足（如果使用GPU）
7. 查看日志文件获取详细错误信息

### 问题4: 移动应用连接失败

**症状**: 移动应用无法连接到API服务器

**解决方案**:
1. 检查API服务器地址配置是否正确
2. 检查网络连接是否正常
3. 检查防火墙设置
4. 检查API服务器是否正在运行

### 问题5: 生成速度慢

**症状**: 生成播客需要很长时间

**解决方案**:
1. 使用GPU加速(推荐，系统会自动检测并启用)
2. 使用FP16精度(默认启用，如果GPU支持)
3. 使用CUDA内核加速(默认启用，如果GPU支持)
4. 减少文本长度
5. 减少角色数量
6. 减少并发任务数量（通过`MAX_CONCURRENT_PODCAST_TASKS`环境变量控制）
7. 使用快思考模式（`HUNYUAN_FAST_THINKING=True`）

### 问题6: 音色不一致

**症状**: 同一角色的音色在不同对话中不一致

**解决方案**:
1. 使用相同的音色参考音频
2. 检查音色文件质量
3. 确保音色文件清晰、无噪音
4. 使用较长的音色参考音频(推荐20-30秒)

### 问题7: 文本格式错误

**症状**: 生成的播客不符合预期

**解决方案**:
1. 检查文本格式是否正确
2. 确保角色标记格式正确
3. 确保情绪标注格式正确
4. 参考示例文本格式

### 问题8: 背景音乐问题

**症状**: 背景音乐无法添加或音量不合适

**解决方案**:
1. 检查本地音乐目录是否存在（`music/`或`index-tts/music/`）
2. 检查云存储音乐配置（如果使用云存储）
3. 调整背景音乐音量参数(`background_volume`, 范围0.0-1.0)
4. 系统会自动选择背景音乐，也可以通过设置`category`参数帮助选择
5. 检查音频合成设置

### 问题9: 音色文件下载失败

**症状**: 系统无法从云存储URL下载音色文件

**解决方案**:
1. 检查云存储URL是否可公开访问（403 Forbidden错误）
2. 检查华为AGC云存储的安全规则配置
3. 确保URL格式正确（完整的HTTP/HTTPS URL）
4. 检查文件是否存在（404 Not Found错误）
5. 检查网络连接是否正常

### 问题10: 批量任务处理慢

**症状**: 批量任务处理很慢或卡住

**解决方案**:
1. 检查并发任务数量限制（默认最多2个并发）
2. 通过`MAX_CONCURRENT_PODCAST_TASKS`环境变量调整并发数
3. 使用多进程模式（`--workers 2`）可以提高进度查询的并发能力
4. 检查GPU内存是否充足
5. 减少单次批量任务数量（建议不超过50个）


## 技术支持

如有问题或建议,请通过以下方式联系:

- **Email**: gavintao@petalmail.com
- **文档**: 参考[技术报告](技术报告.md)和[API文档](API_QUICKSTART.md)

## 更新日志

### v1.1.0 (2025-01)

- ✅ 支持多种输入类型（文字、文件、公众号、网页、PDF、Word）
- ✅ 实现批量生成功能（最多50个任务）
- ✅ 实现文本分析API
- ✅ 实现进度查询和SSE流式进度推送
- ✅ 实现音色文件上传API
- ✅ 优化背景音乐自动选择算法
- ✅ 支持任务管理和历史记录
- ✅ 优化GPU加速（自动检测并启用FP16和CUDA内核）
- ✅ 支持多进程模式（提高并发能力）
- ✅ 改进错误处理和日志记录

### v1.0.0 (2025-01)

- ✅ 实现多角色互动播客生成功能
- ✅ 实现自定义角色人设播客生成功能
- ✅ 实现主题深度播客生成功能
- ✅ 实现Web UI界面
- ✅ 实现REST API服务
- ✅ 实现鸿蒙移动应用
- ✅ 支持Cloud Studio云端部署
- ✅ 支持华为AGC云存储和云数据库
- ✅ 集成腾讯混元大模型API

---

**最后更新**: 2025年12月

**文档版本**: v1.1







