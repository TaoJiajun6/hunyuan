# AI播客生成系统部署说明

## 目录

- [部署概述](#部署概述)
- [本地部署](#本地部署)
- [云端部署](#云端部署)
- [移动应用部署](#移动应用部署)
- [配置说明](#配置说明)
- [故障排查](#故障排查)

## 部署概述

本系统支持三种部署方式:

1. **本地部署**: 在本地计算机上部署,适合开发和测试
2. **云端部署**: 在腾讯云Cloud Studio上部署,适合生产环境
3. **移动应用部署**: 在HarmonyOS设备上部署移动应用

## 本地部署

### 1. 环境准备

#### 系统要求

- **操作系统**: Linux(Ubuntu 20.04+)或Windows 10/11
- **Python**: Python 3.10+
- **CUDA**: CUDA 11.8+(如果使用GPU,推荐)
- **内存**: 32GB RAM(推荐)
- **存储**: 100GB可用存储空间(用于模型文件)

#### 安装依赖

**使用conda环境(推荐)**

```bash
# 1. 克隆仓库
git clone https://github.com/your-username/hunyuan-podcast.git
cd hunyuan

# 2. 创建conda环境
conda create -n hunyuan python=3.10

# 3. 激活环境
conda activate hunyuan

# 4. 安装依赖
pip install -r requirements_podcast.txt
```

**注意**: 以后每次使用系统前，都需要先激活conda环境: `conda activate hunyuan`

#### 下载模型文件

```bash
# 下载SoulX-Podcast模型
pip install -U huggingface_hub
huggingface-cli download --resume-download Soul-AILab/SoulX-Podcast-1.7B --local-dir SoulX-Podcast/pretrained_models/SoulX-Podcast-1.7B
```

#### 配置API密钥

**方式一: 使用环境变量(推荐)**

创建`.env`文件(在项目根目录):

```bash
# 混元大模型API配置
HUNYUAN_API_KEY=your-api-key  # 从 https://console.cloud.tencent.com/hunyuan/start 获取
HUNYUAN_MODEL=hunyuan-a13b  # 可选，默认值
HUNYUAN_FAST_THINKING=True  # 可选，默认开启快思考模式

# 华为AGC云存储配置(可选)
AGC_STORAGE_URL=https://ops-server-drcn.agcstorage.link/v0/
AGC_BUCKET=your-bucket-name
AGC_CLIENT_ID=your-client-id
AGC_CLIENT_SECRET=your-client-secret
AGC_PRODUCT_ID=your-product-id

# 华为AGC云数据库配置(可选)
AGC_API_KEY=your-api-key
AGC_CLOUD_DB_ZONE=cloudDBZone
```

**方式二: 直接在config.py中配置**

在`hunyuan_podcast/config.py`中直接设置:

```python
HUNYUAN_API_KEY = "your-api-key"  # 从 https://console.cloud.tencent.com/hunyuan/start 获取
```

**注意**: API密钥需要从腾讯云混元控制台获取，访问: https://console.cloud.tencent.com/hunyuan/start

### 2. 启动服务

#### 启动Web UI

```bash
python run_podcast_webui.py
```

访问: `http://localhost:7861`

#### 启动API服务

```bash
# 基本启动（自动检测GPU并启用优化）
python run_api_server.py --host 0.0.0.0 --port 8000

# 启用多进程模式（支持并发处理）
python run_api_server.py --host 0.0.0.0 --port 8000 --workers 2

# 禁用FP16（如果GPU不支持）
python run_api_server.py --host 0.0.0.0 --port 8000 --no-fp16

# 禁用CUDA内核加速
python run_api_server.py --host 0.0.0.0 --port 8000 --no-cuda-kernel
```

**启动参数说明**:
- `--host`: 服务主机地址，默认0.0.0.0(允许外部访问)
- `--port`: 服务端口，默认8000
- `--fp16`: 启用FP16精度（GPU加速，自动检测GPU时默认启用）
- `--no-fp16`: 禁用FP16精度
- `--cuda_kernel`: 启用CUDA内核加速（GPU加速，自动检测GPU时默认启用）
- `--no-cuda-kernel`: 禁用CUDA内核加速
- `--workers`: 工作进程数，>=2时可并发处理进度查询

**访问地址**:
- API文档: `http://localhost:8000/docs`
- 健康检查: `http://localhost:8000/health`
- 进度查询: `GET /api/v1/podcast/progress/{job_id}`
- 进度流式推送: `GET /api/v1/podcast/progress/{job_id}/stream`

### 3. 验证部署

#### 健康检查

```bash
curl http://localhost:8000/health
```

预期响应:
```json
{
  "status": "healthy",
  "service": "混元AI播客生成API"
}
```

#### 功能测试

1. 访问Web UI: `http://localhost:7861`
2. 选择播客生成功能
3. 输入测试文本
4. 生成播客音频
5. 验证生成结果

## 云端部署

### 1. Cloud Studio部署

Cloud Studio是腾讯云提供的在线开发环境，支持GPU加速，非常适合部署API服务。

#### 环境准备

1. **创建Workspace**
   - 登录[腾讯云Cloud Studio](https://cloudstudio.net/)
   - 创建新的Workspace
   - 选择GPU模板（如果可用）或标准模板
   - 建议至少8GB内存，20GB存储空间

2. **检查GPU可用性**（可选）
   ```bash
   # 检查GPU是否可用
   python -c "import torch; print(f'CUDA可用: {torch.cuda.is_available()}')"
   
   # 查看GPU信息
   nvidia-smi
   ```

#### 项目部署

1. **上传代码**
   - 使用Git克隆项目：
     ```bash
     git clone https://github.com/your-username/hunyuan-podcast.git
     cd hunyuan
     ```
   - 或使用Cloud Studio的文件上传功能上传项目代码

2. **安装依赖**
   ```bash
   # 创建conda环境（推荐）
   conda create -n hunyuan python=3.10
   conda activate hunyuan
   
   # 安装Python依赖
   pip install -r requirements_podcast.txt
   ```

3. **下载模型文件**
   ```bash
   # 下载SoulX-Podcast模型
   pip install -U huggingface_hub
   huggingface-cli download --resume-download Soul-AILab/SoulX-Podcast-1.7B --local-dir SoulX-Podcast/pretrained_models/SoulX-Podcast-1.7B
   ```
   
   确保模型目录存在：`SoulX-Podcast/pretrained_models/SoulX-Podcast-1.7B/`

4. **配置环境变量**
   
   **方式一: 创建.env文件(推荐)**
   
   在项目根目录创建`.env`文件:
   ```bash
   # 混元大模型API配置
   HUNYUAN_API_KEY=your-api-key  # 从 https://console.cloud.tencent.com/hunyuan/start 获取
   HUNYUAN_MODEL=hunyuan-a13b
   HUNYUAN_FAST_THINKING=True
   
   # 华为AGC云存储配置(可选)
   AGC_STORAGE_URL=https://ops-server-drcn.agcstorage.link/v0/
   AGC_BUCKET=your-bucket-name
   AGC_CLIENT_ID=your-client-id
   AGC_CLIENT_SECRET=your-client-secret
   AGC_PRODUCT_ID=your-product-id
   
   # 华为AGC云数据库配置(可选)
   AGC_API_KEY=your-api-key
   AGC_CLOUD_DB_ZONE=cloudDBZone
   ```
   
   **方式二: 使用环境变量**
   ```bash
   # 设置HuggingFace镜像（可选）
   export HF_ENDPOINT="https://hf-mirror.com"
   
   # 设置API密钥
   export HUNYUAN_API_KEY="your-api-key"
   ```

#### 启动API服务

**方式一：直接运行（推荐）**
```bash
# 基本启动（自动检测GPU并启用优化）
python run_api_server.py --host 0.0.0.0 --port 8000

# 启用多进程模式（支持并发处理进度查询）
python run_api_server.py --host 0.0.0.0 --port 8000 --workers 2

# CPU模式（如果GPU不可用）
python run_api_server.py --host 0.0.0.0 --port 8000 --no-fp16 --no-cuda-kernel
```

启动脚本会自动：
- 检测GPU并自动启用FP16和CUDA内核加速（如果GPU可用）
- 显示本地和Cloud Studio预览地址
- 显示API端点和访问信息

**方式二：使用nohup后台运行**
```bash
# 创建日志目录
mkdir -p logs

# 后台运行（自动GPU优化）
nohup python run_api_server.py --host 0.0.0.0 --port 8000 > logs/api_server.log 2>&1 &

# 查看日志
tail -f logs/api_server.log
```

**方式三：使用nohup后台运行**
```bash
# 创建日志目录
mkdir -p logs

# 后台运行
nohup python run_api_server.py --host 0.0.0.0 --port 8000 --fp16 --cuda_kernel > logs/api_server.log 2>&1 &

# 查看日志
tail -f logs/api_server.log
```

**启动参数说明**：
- `--host 0.0.0.0`: 监听所有网络接口
- `--port 8000`: API服务端口（Cloud Studio端口转发使用此端口）
- `--fp16`: 使用FP16精度（GPU加速，减少显存占用，检测到GPU时默认启用）
- `--no-fp16`: 禁用FP16精度
- `--cuda_kernel`: 使用CUDA内核加速（进一步提速，检测到GPU时默认启用）
- `--no-cuda-kernel`: 禁用CUDA内核加速
- `--workers N`: 工作进程数（>=2时可并发处理进度查询）

#### 端口转发配置

Cloud Studio会自动提供端口转发服务，地址格式为：
```
https://${SPACE_KEY}--${PORT}.${REGION}.cloudstudio.work/
```

**获取SPACE_KEY和REGION的方法**：

1. **从浏览器地址栏获取**（推荐）
   - 查看浏览器地址栏，找到类似：`https://hfrsgm.ap-guangzhou.cloudstudio.work/`
   - 提取SPACE_KEY：`hfrsgm`（第一段）
   - 提取REGION：`ap-guangzhou`（第二段）

2. **从环境变量获取**
   ```bash
   # 查看环境变量
   echo $X_IDE_SPACE_KEY
   echo $REGION
   
   # 或查看所有Cloud Studio相关环境变量
   env | grep -i cloud
   ```

3. **从启动日志获取**
   - 启动API服务器后，查看启动日志
   - 日志中会自动显示端口转发地址

**构建API地址**：
```
https://${SPACE_KEY}--8000.${REGION}.cloudstudio.work/
```

**示例**：
- SPACE_KEY: `hfrsgm`
- REGION: `ap-guangzhou`
- 端口: `8000`
- API地址: `https://hfrsgm--8000.ap-guangzhou.cloudstudio.work/`

#### 验证部署

**健康检查**：
```bash
curl https://hfrsgm--8000.ap-guangzhou.cloudstudio.work/health
```

预期响应：
```json
{
  "status": "healthy",
  "service": "混元AI播客生成API"
}
```

**API文档**：
在浏览器中访问：
```
https://hfrsgm--8000.ap-guangzhou.cloudstudio.work/docs
```

**主要API端点**：
- 多角色播客: `POST /api/v1/podcast/multi_role`
- 自定义角色: `POST /api/v1/podcast/character`
- 主题深度播客: `POST /api/v1/podcast/deep`
- 文本分析: `POST /api/v1/podcast/analyze`
- 批量生成: `POST /api/v1/podcast/batch`
- 上传音色: `POST /api/v1/podcast/upload_voice`
- 查询进度: `GET /api/v1/podcast/progress/{job_id}`
- 流式进度: `GET /api/v1/podcast/progress/{job_id}/stream`

#### Cloud Studio部署常见问题

**问题1：无法访问API服务器**
- 检查API服务器是否正在运行
- 检查端口是否正确（应该是8000）
- 检查SPACE_KEY和REGION是否正确
- 检查Cloud Studio的端口转发服务是否正常

**问题2：GPU不可用**
- 检查Cloud Studio是否提供GPU资源
- 检查CUDA是否正确安装
- 检查PyTorch是否支持CUDA
- 使用CPU模式运行（去掉`--fp16`和`--cuda_kernel`参数）

**问题3：文件上传失败**
- 检查文件大小是否超过限制
- 检查网络连接是否稳定
- 检查API服务器日志查看详细错误

**问题4：生成失败**
- 检查API密钥是否正确配置
- 检查模型文件是否完整
- 检查服务器日志查看详细错误
- 检查GPU内存是否足够

**问题5：超时错误**
- 增加超时时间（修改客户端`API_TIMEOUT`）
- 检查网络连接是否稳定
- 检查服务器性能是否足够
- 优化生成参数

### 2. 其他云平台部署

#### Docker部署

创建Dockerfile:

```dockerfile
FROM pytorch/pytorch:2.0.1-cuda11.8-cudnn8-runtime

WORKDIR /app

# 安装依赖
COPY requirements_podcast.txt .
RUN pip install -r requirements_podcast.txt

# 复制代码
COPY . .

# 启动服务
CMD ["python", "run_api_server.py", "--host", "0.0.0.0", "--port", "8000"]
```

构建和运行:

```bash
# 构建镜像
docker build -t hunyuan-podcast .

# 运行容器
docker run -d -p 8000:8000 \
  -e HUNYUAN_API_KEY="your-api-key" \
  -e HUNYUAN_MODEL="hunyuan-a13b" \
  -e HUNYUAN_FAST_THINKING="True" \
  -v $(pwd)/SoulX-Podcast/pretrained_models:/app/SoulX-Podcast/pretrained_models \
  -v $(pwd)/outputs:/app/outputs \
  --gpus all \
  hunyuan-podcast
```

**注意**: 
- 如果使用GPU，需要添加`--gpus all`参数
- 建议挂载outputs目录以持久化生成的播客文件
- API密钥等配置也可以通过环境变量或.env文件传入

## 移动应用部署

### 1. 配置API地址

在`podcasters/components/lib_api/src/main/ets/services/PodcastService.ets`中配置:

```typescript
export class PodcastConfig {
  // 使用Cloud Studio端口转发地址
  static readonly API_BASE_URL: string = 'https://hfrsgm--8000.ap-guangzhou.cloudstudio.work';
  static readonly API_TIMEOUT: number = 600000; // 10分钟超时
  static readonly USE_CLOUD_FUNCTION: boolean = false;
}
```

### 2. 编译应用

#### 使用DevEco Studio

1. 打开DevEco Studio
2. 导入`podcasters`项目
3. 配置签名和API地址
4. 编译运行应用

#### 配置签名

1. 在AppGallery Connect创建应用
2. 获取APP ID和签名证书
3. 在DevEco Studio中配置签名

### 3. 安装应用

#### 真机调试

1. 使用USB连接HarmonyOS设备
2. 在DevEco Studio中运行应用
3. 在设备上安装应用

#### 发布应用

1. 编译Release版本
2. 上传到AppGallery Connect
3. 提交审核
4. 发布应用

## 配置说明

### 1. API配置

**方式一: 使用.env文件(推荐)**

在项目根目录创建`.env`文件:

```bash
# 混元大模型API配置
HUNYUAN_API_KEY=your-api-key  # 从 https://console.cloud.tencent.com/hunyuan/start 获取
HUNYUAN_MODEL=hunyuan-a13b  # 可选，默认值
HUNYUAN_FAST_THINKING=True  # 可选，默认开启快思考模式
```

**方式二: 在config.py中配置**

在`hunyuan_podcast/config.py`中配置:

```python
# 腾讯云混元API配置
HUNYUAN_API_KEY = "your-api-key"  # 从 https://console.cloud.tencent.com/hunyuan/start 获取
HUNYUAN_API_BASE = "https://api.hunyuan.cloud.tencent.com/v1"
HUNYUAN_MODEL = "hunyuan-a13b"  # 混元混合推理模型
HUNYUAN_FAST_THINKING = True  # 快思考模式，提升速度
```

### 2. 模型配置

```python
# SoulX-Podcast配置
SOULX_PODCAST_MODEL_DIR = "SoulX-Podcast/pretrained_models/SoulX-Podcast-1.7B"
SOULX_PODCAST_LLM_ENGINE = "hf"  # 仅支持 "hf" 引擎（vllm 支持已移除）
SOULX_PODCAST_FP16_FLOW = True  # 使用FP16精度（GPU加速）
```

### 3. 音频配置

```python
# 音频合成配置
AUDIO_SILENCE_INTERVAL = 800  # 角色切换时的静音间隔(毫秒)，默认800ms
AUDIO_SAMPLING_RATE = 22050   # 采样率(Hz)
```

### 4. 服务器配置

#### 端口配置

```bash
# Web UI端口
python run_podcast_webui.py --port 7861

# API服务端口
python run_api_server.py --host 0.0.0.0 --port 8000
```

#### 超时配置

在API服务中配置超时时间:

```python
# 在api_server.py中配置
API_TIMEOUT = 600  # 10分钟超时
```

### 5. 云存储配置

#### 华为AGC云存储

**方式一: 使用.env文件(推荐)**

在项目根目录的`.env`文件中配置:

```bash
# 华为AGC云存储配置
AGC_STORAGE_URL=https://ops-server-drcn.agcstorage.link/v0/
AGC_BUCKET=your-bucket-name
AGC_CLIENT_ID=your-client-id
AGC_CLIENT_SECRET=your-client-secret
AGC_PRODUCT_ID=your-product-id
AGC_DOMAIN=connect-api.cloud.huawei.com
```

**方式二: 使用环境变量**

```bash
export AGC_STORAGE_URL="https://ops-server-drcn.agcstorage.link/v0/"
export AGC_BUCKET="your-bucket-name"
export AGC_CLIENT_ID="your-client-id"
export AGC_CLIENT_SECRET="your-client-secret"
export AGC_PRODUCT_ID="your-product-id"
```

详细配置说明请参考: [CLOUD_STORAGE_INTEGRATION.md](podcasters/CLOUD_STORAGE_INTEGRATION.md)

### 6. 云数据库配置

#### 华为AGC CloudDB

**方式一: 使用.env文件(推荐)**

在项目根目录的`.env`文件中配置:

```bash
# 华为AGC云数据库配置
AGC_API_KEY=your-api-key
AGC_CLOUD_DB_ZONE=cloudDBZone
```

详细配置说明请参考: [CLOUDDB_SETUP.md](CLOUDDB_SETUP.md)

## 故障排查

### 1. 模型加载失败

**症状**: 启动时提示模型加载失败

**解决方案**:
1. 检查模型文件是否已下载
2. 检查模型路径配置是否正确
3. 检查CUDA是否可用(如果使用GPU)
4. 检查依赖是否已安装

### 2. API调用失败

**症状**: API调用时返回错误

**解决方案**:
1. 检查API密钥是否有效（腾讯云混元API密钥）
2. 检查网络连接是否正常
3. 检查API调用额度是否充足
4. 检查API服务器地址是否正确
5. 检查请求参数格式是否正确（特别是`role_voice_urls`必须是云存储URL）
6. 查看API返回的错误信息，根据错误提示进行修复

### 3. 云端部署问题

**症状**: Cloud Studio部署失败

**解决方案**:
1. 检查Workspace配置是否正确
2. 检查GPU实例是否可用
3. 检查端口转发是否配置正确
4. 检查防火墙设置

### 4. 移动应用连接失败

**症状**: 移动应用无法连接到API服务器

**解决方案**:
1. 检查API服务器地址配置是否正确
2. 检查网络连接是否正常
3. 检查防火墙设置
4. 检查API服务器是否正在运行

### 5. 性能问题

**症状**: 生成速度慢或资源消耗大

**解决方案**:
1. 使用GPU加速(推荐，系统会自动检测并启用)
2. 使用FP16精度(默认启用，如果GPU支持)
3. 使用CUDA内核加速(默认启用，如果GPU支持)
4. 减少文本长度
5. 减少角色数量
6. 减少并发任务数量（通过`MAX_CONCURRENT_PODCAST_TASKS`环境变量控制）
7. 使用快思考模式（`HUNYUAN_FAST_THINKING=True`）
8. 启用多进程模式（`--workers 2`）提高进度查询并发能力

## 性能优化

### 1. GPU加速

使用GPU加速可以大大提高生成速度:

```bash
# 检查CUDA是否可用
python -c "import torch; print(torch.cuda.is_available())"

# 使用GPU启动服务（自动检测并启用优化）
python run_api_server.py --host 0.0.0.0 --port 8000

# 或显式指定GPU优化
python run_api_server.py --host 0.0.0.0 --port 8000 --fp16 --cuda_kernel

# 使用多进程模式提高并发能力
python run_api_server.py --host 0.0.0.0 --port 8000 --workers 2
```

系统会自动检测GPU并在可用时启用FP16和CUDA内核加速，无需手动指定。

### 2. 模型量化

使用FP16精度可以在保持质量的同时提高速度:

```python
# 在config.py中配置（默认已启用）
SOULX_PODCAST_FP16_FLOW = True

# 或使用环境变量
export SOULX_PODCAST_FP16_FLOW="True"
```

系统在检测到GPU时会自动启用FP16精度，无需手动配置。

### 3. 缓存机制

启用缓存机制可以减少重复计算:

```python
# 在podcast_generator.py中启用缓存
self.prompt_cache: Dict[str, Dict] = {}  # 缓存音色特征
```

### 4. 批量处理

支持批量处理可以提高吞吐量:

```python
# 在api_server.py中支持批量处理
@app.post("/api/v1/podcast/batch")
async def generate_batch_podcasts(...):
    # 批量处理逻辑
    pass
```

## 监控和维护

### 1. 日志记录

启用日志记录可以帮助诊断问题:

```python
# 在api_server.py中配置日志
import logging
logging.basicConfig(level=logging.INFO)
logger = logging.getLogger(__name__)
```

### 2. 性能监控

监控系统性能可以帮助优化:

```python
# 记录生成时间
import time
start_time = time.time()
# ... 生成逻辑 ...
end_time = time.time()
logger.info(f"生成时间: {end_time - start_time:.2f}秒")
```

### 3. 错误处理

完善的错误处理可以提高系统稳定性:

```python
# 在api_server.py中处理错误
try:
    # 生成逻辑
    pass
except Exception as e:
    logger.error(f"生成失败: {str(e)}")
    return {"error": str(e)}
```

## 安全建议

### 1. API密钥安全

- 不要将API密钥提交到代码仓库
- 使用环境变量存储API密钥
- 定期轮换API密钥

### 2. 访问控制

- 配置防火墙规则
- 使用HTTPS加密传输
- 实现身份认证和授权

### 3. 数据安全

- 加密敏感数据
- 定期备份数据
- 实现数据访问控制

## 更新和维护

### 1. 更新依赖

定期更新依赖包:

```bash
pip install --upgrade -r requirements_podcast.txt
```

### 2. 更新模型

定期更新模型文件:

```bash
# 设置HuggingFace镜像（可选，加速下载）
export HF_ENDPOINT="https://hf-mirror.com"

# 下载模型
huggingface-cli download --resume-download Soul-AILab/SoulX-Podcast-1.7B --local-dir SoulX-Podcast/pretrained_models/SoulX-Podcast-1.7B

# 或使用Python
python -c "from huggingface_hub import snapshot_download; snapshot_download('Soul-AILab/SoulX-Podcast-1.7B', local_dir='SoulX-Podcast/pretrained_models/SoulX-Podcast-1.7B')"
```

### 3. 备份数据

定期备份重要数据:

```bash
# 备份模型文件
tar -czf models_backup.tar.gz SoulX-Podcast/pretrained_models/

# 备份配置文件
cp hunyuan_podcast/config.py config_backup.py
```

## 技术支持

如有问题或建议,请通过以下方式联系:

- **Email**: gavintao@petalmail.com
- **文档**: 参考[技术报告](技术报告.md)和[使用说明](使用说明.md)

---

**最后更新**: 2025年12月

**文档版本**: v1.1







